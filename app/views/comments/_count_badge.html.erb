<span id="<%= dom_id(post, :comment_count) %>" class="inline-flex items-center gap-2 rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-700">
  <span class="h-2 w-2 rounded-full bg-sky-500"></span>
  <%= pluralize(post.comments.count, "comment") %>
</span>
