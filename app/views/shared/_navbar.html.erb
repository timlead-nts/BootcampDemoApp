<nav class="border-b border-slate-200 bg-white/90 backdrop-blur">
  <div class="mx-auto flex h-14 max-w-5xl items-center justify-between px-4 sm:px-6 lg:px-8">
    <div class="flex items-center gap-3">
      <div class="flex h-10 w-10 items-center justify-center rounded-xl bg-gradient-to-br from-sky-500 to-blue-600 text-sm font-bold uppercase text-white shadow-sm shadow-sky-200">
        BC
      </div>
      <div class="leading-tight">
        <%= link_to "Bootcamp Posts", posts_path, class: "block text-base font-semibold text-slate-900 transition hover:text-sky-700" %>
        <p class="text-xs text-slate-500">Ship small updates often.</p>
      </div>
    </div>

    <div class="flex items-center gap-3">
      <% if user_signed_in? %>
        <div class="hidden h-10 items-center gap-3 rounded-xl border border-slate-200 bg-white/80 px-3 py-1.5 shadow-sm shadow-sky-50 sm:flex">
          <div class="flex h-8 w-8 items-center justify-center rounded-lg bg-sky-50 text-xs font-semibold uppercase tracking-wide text-sky-700">
            <%= current_user.username.first(2).presence || "U" %>
          </div>
          <div class="leading-tight">
            <p class="text-sm font-semibold text-slate-900"><%= current_user.username %></p>
            <p class="text-[11px] text-slate-500"><%= current_user.email %></p>
          </div>
        </div>
        <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "hidden rounded-lg border border-slate-200 bg-white px-3 py-1.5 text-sm font-semibold text-slate-700 shadow-sm transition hover:border-sky-200 hover:text-sky-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-sky-500 sm:inline-flex" %>
      <% else %>
        <%= link_to "Log in", new_user_session_path, class: "hidden text-sm font-semibold text-sky-700 transition hover:text-sky-800 sm:inline-flex" %>
      <% end %>
    </div>
  </div>
</nav>
